services:
  relay:
    image: ghcr.io/autoconfig/uapi-relay:latest
    container_name: uapi-relay
    restart: unless-stopped
    volumes:
      - ./config.relay.yaml:/app/config.yaml:ro
    ports:
      - "127.0.0.1:8081:8081"

networks:
  default:
    name: uapi-relay-net
