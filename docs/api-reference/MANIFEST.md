# API Reference Manifest

Required categories: `openai-chat-completions`, `openai-responses`, `gemini-api`, `anthropic-messages`.

The corpus is freshly downloaded and organized by official path structure. Run `./verify.py`.
